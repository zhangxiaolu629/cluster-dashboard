# CLAUDE.md

## 开发规范

### Bug Fix 规范
以后bugfix，完成后都要说明问题在哪+怎么解决的

### 代码审查规范
在进行代码审查时，请优先阅读 `docs/code-review/` 中的规则，特别是 `docs/code-review/nextjs-checklist.md`。输出格式请遵循 `docs/code-review/README.md` 的约定。

### 会话归档规范
每次重要会话结束后，统一保存到 `docs/specstory/`。
